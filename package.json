{
  "name": "core-blog-api",
  "version": "1.0.0",
  "type": "module",
  "private": false,
  "description": "Artificialis Lab — Blog API modular (auth + CRUD posts + upload capas). Stack genérico, provisionado automaticamente via painel admin pro cliente final.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^8.4.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.1.1",
    "pg": "^8.13.1",
    "sanitize-html": "^2.17.3"
  }
}
